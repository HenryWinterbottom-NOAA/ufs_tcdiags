# Define the path to the YAML-formatted file containing the input
# analysis variable attributes.
inputs: /ufs_tcdiags/parm/inputs.gfs.yaml

# Define the path to the YAML-formatted file containing the attributes
# for the respective TC events to be analyzed.
tcinfo: /ufs_tcdiags/parm/tcinfo.yaml

